import { FirstWordCapitalPipe } from './first-word-capital.pipe';

describe('FirstWordCapitalPipe', () => {
  it('create an instance', () => {
    const pipe = new FirstWordCapitalPipe();
    expect(pipe).toBeTruthy();
  });
});
